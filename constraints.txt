# The author's exact working set: a full `pip freeze` of the virtualenv this
# tool is developed and run in, pinning every package in the tree rather than
# only the six direct dependencies. Those, with lower bounds, live in
# pyproject.toml -- this file is not a dependency list and installing it alone
# does not install the tool.
#
# It is entirely opt-in. The normal install is:
#
#     pip install -e ".[dev]"
#
# Reach for this file only when an upstream release has broken something and
# you want the set known to work while you find out what:
#
#     pip install -e . -c constraints.txt
#
# CI deliberately does NOT apply it. Installing unpinned is what turns an
# upstream release that breaks rendering into a red build within a day,
# instead of a surprise months later when someone finally lifts the pin.
#
# To update: install whatever you want into the venv and freeze again.

certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
cryptography==50.0.0
google-analytics-admin==0.30.1
google-analytics-data==0.23.0
google-api-core==2.34.0
google-auth==2.56.3
googleapis-common-protos==1.75.1
grpcio==1.83.0
grpcio-status==1.83.0
idna==3.18
markdown-it-py==4.2.0
mdurl==0.1.2
plotext==5.3.2
proto-plus==1.28.3
protobuf==7.35.1
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
Pygments==2.20.0
python-dotenv==1.2.2
requests==2.34.2
rich==15.0.0
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
