# The whole tree pinned (pip freeze), not only the direct dependencies: with no
# lockfile, the freeze is what makes a future install reproduce this one.
#
# Direct — everything else is transitive:
#   google-analytics-data   GA4 Data API (reports and realtime)
#   google-analytics-admin  the property's name and timezone
#   plotext                 charts in the terminal
#   rich                    terminal layout
#   python-dotenv           reads .env
#   tzdata                  the IANA timezone database. Windows ships none, and
#                           without it ZoneInfo('America/Sao_Paulo') raises
#                           ZoneInfoNotFoundError — the whole day-boundary
#                           calculation depends on it.
#
# To update: install whatever you want into the venv and freeze again.

certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
cryptography==50.0.0
google-analytics-admin==0.30.1
google-analytics-data==0.23.0
google-api-core==2.34.0
google-auth==2.56.3
googleapis-common-protos==1.75.1
grpcio==1.83.0
grpcio-status==1.83.0
idna==3.18
markdown-it-py==4.2.0
mdurl==0.1.2
plotext==5.3.2
proto-plus==1.28.3
protobuf==7.35.1
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
Pygments==2.20.0
python-dotenv==1.2.2
requests==2.34.2
rich==15.0.0
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
